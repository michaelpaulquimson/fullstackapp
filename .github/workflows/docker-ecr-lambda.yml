name: Build and Push Docker Image to ECR (on version bump)

on:
  push:
    paths:
      - 'backend/package.json'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    env:
      AWS_REGION: us-east-1
      ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Get version from package.json
        id: get_version
        run: |
          VERSION=$(jq -r .version backend/package.json)
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Build Docker image
        run: |
          docker build -t $ECR_REPOSITORY:${{ steps.get_version.outputs.version }} ./backend

      - name: Push Docker image to ECR
        run: |
          docker push $ECR_REPOSITORY:${{ steps.get_version.outputs.version }}

      - name: Output image URI
        run: echo "Pushed image: $ECR_REPOSITORY:${{ steps.get_version.outputs.version }}"
